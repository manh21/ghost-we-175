<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="{{asset "built/index.css"}}" />
    <script src="{{asset "built/index.js"}}" defer></script>

    <title>{{meta_title}}</title>

    {{ghost_head}}
    {{!-- Outputs important meta data and settings, should always be in <head> --}}

</head>

<body class="{{body_class}}">
<div class="min-h-screen flex flex-col">

<main class="flex-grow flex items-center justify-center bg-gray-100">

    <section class="text-center p-8 bg-white shadow-md rounded-md">
        <div class="container mx-auto">

            <section>

                <h1 class="text-6xl font-bold text-gray-800">{{statusCode}}</h1>
                <p class="text-xl text-gray-600 mt-4">{{message}}</p>
                <p class="mt-6"><a href="{{@site.url}}" class="text-blue-500 hover:underline">Go to the front page →</a></p>

                {{#if errorDetails}}
                <section class="mt-8 text-left">
                    <h4 class="text-lg font-semibold">Theme errors:</h4>
                    <ul class="mt-4 space-y-2">
                        {{#foreach errorDetails}}
                        <li class="border-t pt-2">
                            <h5 class="font-semibold">{{{rule}}}</h5>

                            {{#foreach failures}}
                            <span class="block"><strong>Ref:</strong> {{ref}}</span>
                            <span class="block"><strong>Message:</strong> {{message}}</span>
                            {{/foreach}}
                        </li>
                        {{/foreach}}
                    </ul>
                </section>
                {{/if}}

            </section>

        </div>
    </section>

</main>

</div>

{{!-- <foot> --}}
{{ghost_foot}}
{{!-- Outputs important scripts - should always be included before closing body tag --}}

</body>
</html>